"""Historic street-name aliases: parse a rename source, propose, sweep, validate.

Import the submodules directly (``from autogeoref.alias import sweep``); this
package deliberately re-exports nothing. ``autogeoref.name_match`` stays
top-level — it is a shared measurement utility, not an alias part.
"""
